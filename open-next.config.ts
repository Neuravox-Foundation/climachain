import { defineCloudflareConfig } from "@opennextjs/cloudflare"

export default defineCloudflareConfig({
  // Add caching, R2 buckets, KV bindings, etc. here when needed.
  // Defaults work for the current ClimaChain stack.
})
